
İlk önce yapılacaklar:
---------------------
    (X) Yeni arayüz tasarımı yapılacak.

    (X) Lisan seçimi penceresi oluştur.
        (X) *.qm çeviri dosyalarını önceden tanımlanan klasörden al.

    (X) Paket inşa bilgisi (*.pisido) iptal edilcek.
    (X) Çalışma Dizini yerine Çalışma Alanı (Workspace) kavramı gelecek. Programın açılışında bir kere sorulacak.

    (X) Stable dalındaki renklendirme ve otomatik tamamlama desteği eklenecek.
        (X) actions.py Action API dosyası için metin editörüne python desteği eklenecek.
            (X) Metin editörüne kod renklendirici eklenecek
            (X) Metin editörüne otomatik tamamlama eklenecek.

    (X) İnşa adımları yeniden tasarlanacak.
        (X) İnşa adımları belirlenecek. Pspec.xml oluşumu, action.py oluşumu,
            arşiv indirilmesi, ... sonrasında pisi nin desteklediği adımlar.

    (X) pspec.xml dosyasının içe/dışa aktarımı sağlanacak.
        (X) pspec bilgisini barındıracak C++ sınıfları tasarlanacak.
            (X) Pisi sınıfıyla pspec.xml dosyasından QDomDocument'e bilgilerin yüklenmesi sağlanacak.
            (X) Pisi sınıfıyla pspec.xml dosyasına arayüzden bilgilerin yüklenmesi sağlanacak.
                (X) save_to_dom();
                    (X) Update
                    (X) Package::Files,
                    (X) Souce::Patches,
                    (X) SPBase::AdditionalFiles

    (X) Konsol için satır limiti konacak.

    (X) QFileSystemWatcher (veya QFileSystemModel) kullanılacak dizinler:
        (X) __workspace__/__package_name__/
        (X) __workspace__/__package_name__/files/
        (X) /var/pisi/__package_name__/install/

    (X) pspec.xml de birden fazla arşivi destekle.

    (X) bash için root hakları alınacak
        pkexec --user root pisi bi pspec.xml --output-dir workspace

    (X) Şunlar için arayüze ve arayüzden doldurma yapılacak.
        (X) Package::Files,
        (X) Souce::Patches,
        (X) SPBase::AdditionalFiles

    (X) İnşa işlemleri yapılacak.
        (X) aditional_files daki __package_name__ dönüştürülecek

    (X) Pisi::save_to_dom() da dom varsa ve yoksa durumlarına göre dom üretilecek.

    (X) gui tab sırası kontrol edilecek.
    (X) pisi sınıflarındaki QString("") ler QObject::tr("") yapılacak.
    (X) istisna fırlatan fonksiyonlara throw eklenecek.

    (X) gui tooltip leri kontrol edilecek.

    (X) directory, folder kelimelerinden biri seçilecek. (directory seçildi)

    (X) ConsoleWidget dan QTermWidget a geçilecek.


    (->) Program test edilecek.

    (*) Program yardımı hazırlanacak.

    (*) Pisi paketinde çeviri dosyaları dizini verilecek.
        (*) qtermwidget bağımlılığı ekle


Sonra yapılacaklar:
-------------------

    (*) translation.xml oluşturulacak.

    (*) actions api için otomatik tamamlama yapılacak.

    (*) pspec.xml gösterim arabirimi eklenecek.

    (*) Source::Patch yapısı arayüze aktarılacak. Şimdilik sadece level aktarıldı.



İşaret haritası:
----------------
    (*) yapılmadı
    (->) yapılıyor
    (X) yapıldı
